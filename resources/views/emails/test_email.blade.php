<!DOCTYPE html>
<html>
<head>
    <title>{{ $data['title'] ?? 'Default Title' }}</title>
</head>
<body>
    <h1>{{ $data['title'] ?? 'Default Title' }}</h1>
    <p>Your email content goes here.</p>
</body>
</html>
